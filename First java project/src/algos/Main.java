package algos;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Scanner;

import javax.sound.midi.SysexMessage;
public class Main {
  public static void main(String[] args) {
	  new algo2();
	/*  Scanner userInput = new Scanner(System.in);  	  
	  int[] n = {
			  0 + (int)(Math.random() * 6),
			  0 + (int)(Math.random() * 6),
			  0 + (int)(Math.random() * 6),
			  0 + (int)(Math.random() * 6)
	  		};
	  System.out.println(Arrays.toString(n));
	  
	  for(int i=0; i <= 10 ;i++) {
		  System.out.println("Enter number:");
		  int[] m = new int[4];
		  m[0] = userInput.nextInt();
		  m[1] = userInput.nextInt();
		  m[2] = userInput.nextInt();
		  m[3] = userInput.nextInt();
		  ArrayList<String> result = new ArrayList<String>(); // Create an ArrayList object
		  result.add(null);
		  result.add(null);
		  result.add(null);
		  result.add(null);
		  for(int x=0; x < n.length; x++) {
			  
			  for(int z=0;z<n.length;z++ ) {
				  if(m[x] == n[z]) {
					  result.set(z,"O");
				  }
			  }
			  if(m[x] == n[x]){
				  result.set(x,"#");
			  }else {	
				  result.set(x,""+m[x]);
			  }
		  }
		  System.out.flush();
		  System.out.println(result);
	  }
	  System.out.println("Game Over");*/
  }
}